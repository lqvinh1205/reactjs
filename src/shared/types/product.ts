export type Product = {
    _id?: any,
    name: string,
    price: number,
    category: string,
    images: string,
    speed: string,
    boost: string,
    technology: string,
    description: string,
}